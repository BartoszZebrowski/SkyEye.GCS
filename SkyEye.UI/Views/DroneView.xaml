<Page x:Class="SkyEye.UI.Views.DroneView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SkyEye.UI.Views"
      xmlns:dataContext="clr-namespace:SkyEye.UI.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="DroneView">
    <Page.Resources>



        <Style TargetType="Button" x:Key="MainButtonStyle">
            <Setter Property="Background" Value="#AA1E1E1E"/>

            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="10, 2, 10, 2"/>

            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Foreground" Value="White"/>

        </Style>

        <Style TargetType="Button" x:Key="ControlButtonStyle">
            <Setter Property="Background" Value="#AA1E1E1E"/>

            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="10, 2, 10, 2"/>

            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Foreground" Value="White"/>

        </Style>

        <Style TargetType="Label" x:Key="MainLabelStyle">

            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="25"/>
            <Setter Property="Foreground" Value="White"/>

        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="190"/>
        </Grid.ColumnDefinitions>

        <Image x:Name="_mediaPlayer" Stretch="Uniform" Grid.ColumnSpan="3" Grid.RowSpan="5" Source="C:\Users\barto\Desktop\droneViewPlaceholder.jpg" Panel.ZIndex="-1"></Image>

        <Canvas x:Name="_hud" Grid.ColumnSpan="3" Grid.RowSpan="5">

            <!--Kolo-->
            <Ellipse x:Name="_gimbalPositionAreaEllipse" Width="350" Height="350" Stroke="#77FFFFFF" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse.RenderTransform>
                    <TranslateTransform x:Name="_gimbalPositionAreaEllipseTransform"/>
                </Ellipse.RenderTransform>
            </Ellipse>

            <Ellipse x:Name="_gimbalPositionDot" Width="5" Height="5" Stroke="#77FFFFFF" Fill="#77FFFFFF" StrokeThickness="0">
                <Ellipse.RenderTransform>
                    <TranslateTransform x:Name="_gimbalPositionDotTransform"/>
                </Ellipse.RenderTransform>
            </Ellipse>

            <!--Celownik-->
            <Line StrokeThickness="2" X1="62" Y1="0" X2="62" Y2="50" Canvas.Left="50" Stroke="#AAFFFFFF"></Line>
            <Line StrokeThickness="2" X1="62" Y1="75" X2="62" Y2="125" Canvas.Left="50" Stroke="#AAFFFFFF"></Line>
            <Line StrokeThickness="2" X1="0" Y1="62" X2="50" Y2="62" Canvas.Left="50" Stroke="#AAFFFFFF"></Line>
            <Line StrokeThickness="2" X1="75" Y1="62" X2="125" Y2="62" Canvas.Left="50" Stroke="#AAFFFFFF"></Line>

        </Canvas>

        <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center" Grid.Row="0" Background="#AA1E1E1E">

                <StackPanel Margin="20, 0, 0, 0" Orientation="Horizontal">
                    <Label  Style="{StaticResource MainLabelStyle}" Content="Vertical:" Foreground="White" FontSize="30" ></Label>
                    <Label  Style="{StaticResource MainLabelStyle}" Content="{Binding VerticalAxis}" Foreground="White" FontSize="30" ></Label>
                </StackPanel>
                <StackPanel Margin="20, 0, 0, 0" Orientation="Horizontal">
                    <Label  Style="{StaticResource MainLabelStyle}" Content="Horisontal:" Foreground="White" FontSize="30"></Label>
                    <Label  Style="{StaticResource MainLabelStyle}" Content="{Binding HorisonalAxis}" Foreground="White" FontSize="30"></Label>
                </StackPanel>
                <StackPanel Margin="20, 0, 0, 0" Orientation="Horizontal">
                    <Label  Style="{StaticResource MainLabelStyle}" Content="Zoom:" Foreground="White" FontSize="30"></Label>
                    <Label  Style="{StaticResource MainLabelStyle}" Content="{Binding ZoomValue}" Foreground="White" FontSize="30"></Label>
                </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" Height="118" Grid.Row="1">
            <Button Width="180" Style="{StaticResource MainButtonStyle}" Content="ManualMode" Command="{Binding ChangeModeCommand}" CommandParameter="ManualMode"/>
            <Button Width="180" Style="{StaticResource MainButtonStyle}" Content="FollowMode" Command="{Binding ChangeModeCommand}" CommandParameter="FollowMode"/>
        </StackPanel>

        <StackPanel Grid.Column="2" Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center" Height="119" Margin="0,-69,0,0">
            <Button Width="180" Style="{StaticResource MainButtonStyle}" Content="ZoomIn" Command="{Binding ChangeZoomCommand}" CommandParameter="0,1"/>
            <Button Width="180" Style="{StaticResource MainButtonStyle}" Content="ZoomOut" Command="{Binding ChangeZoomCommand}" CommandParameter="-0,1"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="2" Grid.RowSpan="1" Orientation="Horizontal" HorizontalAlignment="Center" Width="74">
            <Button Style="{StaticResource MainButtonStyle}" Content="Up" Command="{Binding IncreesAngleControlCommand}" CommandParameter="Up"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="2" Grid.RowSpan="1" Orientation="Horizontal" HorizontalAlignment="Center" Width="184">
            <Button Style="{StaticResource MainButtonStyle}" Content="Left" Command="{Binding IncreesAngleControlCommand}" CommandParameter="Left"/>
            <Button Style="{StaticResource MainButtonStyle}" Content="Right" Command="{Binding IncreesAngleControlCommand}" CommandParameter="Right"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="2" Grid.RowSpan="1" Orientation="Horizontal" HorizontalAlignment="Center" Width="106">
            <Button Style="{StaticResource MainButtonStyle}" Content="Down" Command="{Binding IncreesAngleControlCommand}" CommandParameter="Down"/>
        </StackPanel>
    </Grid>
</Page>
